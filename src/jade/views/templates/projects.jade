.projects.projects_center
    ul.projects__in
        li.projects__item(ng-repeat="project in projects")
            img.icon(ng-src="{{project.iconUrl}}", alt="")
            span(translate="{{project.name}}")